import 'package:flutter/material.dart';

class Tasbiih extends StatelessWidget {
  const Tasbiih({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(),
      body: Center(
        child: Text("Tasbiih"),
      ),
    );
  }
}
