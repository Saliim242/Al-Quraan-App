import 'package:flutter/material.dart';

class AsmaUllahScreen extends StatelessWidget {
  const AsmaUllahScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(),
      body: Center(
        child: Text("Asmaa Ullah Screen"),
      ),
    );
  }
}
