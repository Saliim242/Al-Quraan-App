import 'package:flutter/material.dart';

class Juz extends StatelessWidget {
  const Juz({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(),
      body: Center(
        child: Text("Juz Qur an KAriim"),
      ),
    );
  }
}
